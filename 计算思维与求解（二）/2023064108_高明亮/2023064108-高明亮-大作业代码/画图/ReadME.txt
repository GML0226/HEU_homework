在执行画图程序前，必须保证运行过前五个算法程序，这样才能正常读取到结果并绘图

在执行画图程序中，因为是从其余五种算法的文件夹中读取结果才进行绘图
所以在读取文件中使用了相对路径，但因为在提交代码时文件命名逻辑会有改变
所以老师在运行时需要根据最后的实际文件路径对画图程序中的读取文件部分进行修改
更改为正确的结果数据路径

除此之外，两个cpp文件分别是绘制所有ROC曲线在同一个坐标轴中，和分别绘制各个ROC曲线的程序
